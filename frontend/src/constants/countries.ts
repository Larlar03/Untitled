import CitiesRegion from '../types/cities-regions.ts';

const countries: CitiesRegion[] = [
    { label: 'England', type: 'country' },
    { label: 'Wales', type: 'country' }
];

export default countries;
